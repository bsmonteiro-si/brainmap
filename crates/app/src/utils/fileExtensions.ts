// Keep in sync with handlers.rs::handle_resolve_image_path
export const IMAGE_EXTS = [".png", ".jpg", ".jpeg", ".gif", ".svg", ".webp", ".ico", ".bmp"];

// Keep in sync with handlers.rs::handle_resolve_video_path
export const VIDEO_EXTS = [".mp4", ".webm", ".mov", ".avi", ".mkv", ".ogv"];
